<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HifzApp">

        <activity android:name=".ChallengeActivity" />
        <activity android:name=".SurahListActivity" />
        <activity android:name=".HifzSetupActivity" />
        <activity android:name=".HifzActivity" />
        <activity android:name=".CompletionActivity" />
        <activity android:name=".HifzHistoryActivity" />

        <activity
            android:name=".SettingsActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity android:name=".AboutActivity" />
        <activity android:name=".pageViewActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>